<%= form_for(@student) do |f| %>
  <% if @student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student.errors.count, "error") %> prohibited request from being saved:</h2>

      <ul>
      <% @student.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
	<%= f.label 'E-mail (will not be shared)' %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label 'Prefer the on-line course?' %><br />
    <%= f.check_box :online %>
  </div>
  <div class="field">
    <%= f.label 'comment (optional)' %><br />
    <%= f.text_field :comment %>
  </div>
  <div class="actions">
    <%= f.submit 'submit' %>
  </div>
<% end %>
